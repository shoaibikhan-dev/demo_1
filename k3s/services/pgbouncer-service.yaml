apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: demo-namespace
  labels:
    app: pgbouncer
spec:
  selector:
    app: pgbouncer
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  type: ClusterIP
